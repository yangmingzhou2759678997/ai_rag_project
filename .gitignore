# 文件路径: .gitignore

# ==========================================
# 1. 核心机密拦截 (绝对不能传到公共仓库)
# 作用：防止你的大模型 API_KEY 和数据库密码泄露。一旦泄露，黑客一晚上就能刷掉你几千块钱。
# ==========================================
.env
.env.*
*.pem
*.key

# ==========================================
# 2. Python 虚拟环境与依赖 (不要上传别人的轮子)
# 作用：虚拟环境里装了几百 MB 的第三方包，这些包不需要上传。同事拉取代码后，只需根据 requirements.txt 自己 pip install 即可。
# ==========================================
venv/
.venv/
env/
.env_dir/

# ==========================================
# 3. Python 编译缓存垃圾 (保持仓库纯净)
# 作用：Python 运行时会自动生成这些二进制缓存文件以加速下次运行，它们在不同电脑上不通用，必须拦截。
# ==========================================
__pycache__/
*.py[cod]
*$py.class
*.so

# ==========================================
# 4. 操作系统生成的隐藏垃圾
# 作用：拦截 Mac 或 Windows 系统自动生成的缩略图或文件夹配置文件，避免污染代码库。
# ==========================================
.DS_Store
Thumbs.db
desktop.ini

# ==========================================
# 5. IDE 开发工具配置文件
# 作用：你用 PyCharm，你的同事可能用 VSCode。不要把你自己的编辑器个人喜好配置强加给团队。
# ==========================================
.idea/
.vscode/
*.swp
*.swo

# ==========================================
# 6. 本地日志与运行中产生的业务数据
# 作用：你项目中配置的 logger 如果在本地生成了 .log 文件，或者测试时生成了临时的 sqlite 数据库，千万别传到线上。
# ==========================================
logs/
*.log
*.sqlite
*.db

# ==========================================
# 7. 离线 RAG 知识库原始文件 (视情况拦截)
# 作用：如果在开发 seed_db.py 时，你在本地放了一些保密的 PDF 或 TXT 员工手册用来测试，绝对不能传出去。
# ==========================================
data/
*.pdf
*.txt
*.csv

#8. 自己的
study.py